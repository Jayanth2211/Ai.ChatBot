{
  "name": "ai-chatbot",
  "version": "1.0.0",
  "description": "AI Chatbot Fullstack Application",
  "main": "BackEnd/server.js",
  "scripts": {
    "start": "node BackEnd/server.js",
    "build": "cd FrontEnd/vite-project && pwd && ls -la && npm install && npm run build && echo '=== After build ===' && ls -la && echo '=== Dist contents ===' && ls -la dist/ || echo 'No dist folder'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}