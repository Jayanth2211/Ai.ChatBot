{
  "name": "ai-chatbot",
  "version": "1.0.0",
  "description": "AI Chatbot Fullstack Application",
  "main": "BackEnd/server.js",
  "scripts": {
    "start": "node BackEnd/server.js",
    "dev": "nodemon BackEnd/server.js",
    "build": "cd FrontEnd/vite-project && yarn install && yarn build",
    "install-client": "cd FrontEnd/vite-project && yarn install",
    "render-build": "yarn install-client && yarn build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}